# Generated Files

`passcodeConfig.ts` is generated in GitHub Actions from the `SITE_PASSCODE` repository secret.

The generated file contains only public PBKDF2 parameters: salt, hash, iteration count, and algorithm name. It must not contain the passcode and is excluded from git.
